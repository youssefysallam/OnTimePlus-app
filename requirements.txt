# --- Core RAG stack ---
openai>=1.40.0
langgraph>=0.2.0

# --- Embeddings & vector store ---
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
# Local embedding fallback for providers without an embeddings endpoint
# (e.g. OpenRouter). Pulls in torch on first install.
sentence-transformers>=3.0.0

# --- Data parsing ---
pandas>=2.2.0
numpy>=1.26.0
python-dateutil>=2.9.0
pydantic>=2.7.0
pydantic-settings>=2.4.0

# --- API backend (decoupled, future mobile app) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Web UI ---
streamlit>=1.38.0
requests>=2.32.0

# --- Evaluation ---
scikit-learn>=1.5.0
matplotlib>=3.9.0
tqdm>=4.66.0

# --- Dev ---
python-dotenv>=1.0.0
pytest>=8.3.0
